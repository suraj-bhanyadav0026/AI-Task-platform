# === FILE: infra-repo/k8s/base/redis-service.yaml ===
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: ai-tasks
spec:
  selector:
    app: redis
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
