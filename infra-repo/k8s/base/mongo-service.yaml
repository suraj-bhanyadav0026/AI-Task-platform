# === FILE: infra-repo/k8s/base/mongo-service.yaml ===
apiVersion: v1
kind: Service
metadata:
  name: mongo
  namespace: ai-tasks
spec:
  selector:
    app: mongo
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017
