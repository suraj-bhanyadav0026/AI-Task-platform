# === FILE: infra-repo/k8s/base/backend-service.yaml ===
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: ai-tasks
spec:
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
